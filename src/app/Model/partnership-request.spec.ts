import { PartnershipRequest } from './partnership-request';

describe('PartnershipRequest', () => {
  it('should create an instance', () => {
    expect(new PartnershipRequest()).toBeTruthy();
  });
});
