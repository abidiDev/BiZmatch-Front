import { ConversationRequest } from './conversation-request.model';

describe('ConversationRequest', () => {
  it('should create an instance', () => {
    expect(new ConversationRequest()).toBeTruthy();
  });
});
