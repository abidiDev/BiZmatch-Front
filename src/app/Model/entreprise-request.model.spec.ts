import { EntrepriseRequest } from './entreprise-request.model';

describe('EntrepriseRequest', () => {
  it('should create an instance', () => {
    expect(new EntrepriseRequest()).toBeTruthy();
  });
});
